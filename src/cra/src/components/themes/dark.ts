import { createMuiTheme } from "@material-ui/core";

const newLocal = "#000";
export const darkTheme = createMuiTheme({
  palette: {
    type: "dark",
    primary: {
      main: newLocal
    }
  }
});